<?xml version="1.0" encoding="UTF-8"?>
<configuration>

  <springProperty name="APP_NAME" source="spring.application.name" defaultValue="rates-service"/>
  <springProperty name="ENV" source="spring.profiles.active" defaultValue="local"/>

  <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>
        %d{yyyy-MM-dd HH:mm:ss.SSS}
        %-5level
        [%thread]
        [%X{traceId}]
        %logger{36}
        - %msg%n
      </pattern>
    </encoder>
  </appender>
  <appender name="LOKI" class="com.github.loki4j.logback.Loki4jAppender">

    <http>
      <url>http://localhost:3100/loki/api/v1/push</url>
      <encoder class="com.github.loki4j.logback.json.JsonEncoder"/>
      <connectionTimeoutMs>3000</connectionTimeoutMs>
      <readTimeoutMs>5000</readTimeoutMs>
    </http>

    <format>
      <label>
        <pattern>
          app=${APP_NAME},
          env=${ENV},
          level=%level
        </pattern>
      </label>

      <message>
        <pattern>
          {
          "timestamp":"%d{yyyy-MM-dd'T'HH:mm:ss.SSS}",
          "level":"%level",
          "logger":"%logger",
          "thread":"%thread",
          "traceId":"%X{traceId}",
          "message":"%msg"
          }
        </pattern>
      </message>
    </format>
  </appender>

  <appender name="ASYNC_LOKI" class="ch.qos.logback.classic.AsyncAppender">
    <queueSize>5000</queueSize>
    <discardingThreshold>0</discardingThreshold>
    <neverBlock>true</neverBlock>
    <appender-ref ref="LOKI"/>
  </appender>

  <root level="INFO">
    <appender-ref ref="CONSOLE"/>
    <appender-ref ref="ASYNC_LOKI"/>
  </root>

</configuration>

